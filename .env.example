# Opcjonalne — pod przyszłe podpięcie modelu LLM do nauczyciela AI (lib/ai.ts).
# Aplikacja działa w pełni offline bez tych kluczy.

# Klucz do hostowanego modelu (np. Claude API). NIE commituj prawdziwego klucza.
# VITE_AI_API_BASE=https://twoj-proxy.example.com
# VITE_AI_MODEL=claude-haiku-4-5
